/*
@using Grpc.Core;
@using Grpc.Net.Client;
@using PerfRunner.V1;

<div class="page">
    <!-- EditForm Model="@_actionOption" OnValidSubmit="(item.Guid) => HandleUpdateAction(item.Guid)" -->
    <EditForm Model="@UpdateAction" OnValidSubmit="@HandleUpdateAction">
        <DataAnnotationsValidator />
        <ValidationSummary />
        <label>Guid - @UpdateAction.ActionOption.Guid</label>
        <br>
        <label>Name - @UpdateAction.ActionOption.Name</label>
        <br>
        <label>Rate - @UpdateAction.ActionOption.Rate</label>
        <InputNumber @bind-Value=UpdateAction.ActionOption.Rate></InputNumber>
        <br>
        <label>Duration - @UpdateAction.ActionOption.Duration</label>
        <!-- InputNumber @bind-Value=item_.Duration></InputNumber -->
        <br>
        <label>Load Distribution - @UpdateAction.ActionOption.LoadDistribution</label>
        <!-- InputSelect @bind-Value=item_.LoadDistribution>
            <option value="@LoadDistribution.Even">Even</option>
            <option value="@LoadDistribution.Uneven">Uneven</option>
        </InputSelect -->
        <br>
        Paused:
        <!-- InputCheckBox @bind-Value=_updateAction.ActionOption.Paused / -->
        <br>
        <button type="submit">Update</button>
    </EditForm>
</div>

@code{
    [Parameter]
    public UpdateAction UpdateAction { get; set; } = new UpdateAction();

    // [Parameter]
    // public OnValidSubmit 
}*/
