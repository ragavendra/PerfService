using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace PerfRunner.Network
{
    public class TestRunner
    {
        public void RunTest(){

/*
            IServiceProvider serviceProvider= new ServiceProvider(new )
            IHttp service = serviceProvider.GetRequiredService<IHttp>();
            service*/
        }
    }
}